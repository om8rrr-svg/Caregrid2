# Development Environment Configuration
# Localhost URLs for local development and testing

# API Configuration
API_BASE=http://localhost:3000
PUBLIC_WEB_BASE=http://localhost:8000
N8N_BASE=http://localhost:5678

# Frontend URLs
FRONTEND_URL=http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Database Configuration (Local PostgreSQL)
DATABASE_URL=postgresql://caregrid_user:password@localhost:5432/caregrid_dev
EXTERNAL_DATABASE_URL=postgresql://caregrid_user:password@localhost:5432/caregrid_dev

# Vapi Webhook URLs (Local n8n)
VAPI_TOOLS_WEBHOOK=http://localhost:5678/webhook/caregrid/voice/tools
VAPI_EVENTS_WEBHOOK=http://localhost:5678/webhook/caregrid/voice/events

# CDN Configuration
CDN_BASE_URL=http://localhost:8000

# Email Configuration (Ethereal for testing)
EMAIL_SERVICE=ethereal
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false

# SMS Configuration (Mock/Fake gateway)
SMS_PROVIDER=mock
SMS_API_URL=http://localhost:9999/mock-sms

# Security
NODE_ENV=development
TRUST_PROXY=false
JWT_SECRET=development-jwt-secret-key-not-for-production

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:8000,http://localhost:3000,http://localhost:5678